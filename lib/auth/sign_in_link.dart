import 'package:flutter/material.dart';

class SignInLink extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return RaisedButton(
      onPressed: null,
      child: Text("Sign In With Emailed Link"),
    );
  }
}